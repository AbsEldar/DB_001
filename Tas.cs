namespace DB_001
{
    public enum Tas
    {
        id, name, categories, category_id, description, price, products,
        user_id, orders, created,
        order_id, product_id, order_product
    }
}